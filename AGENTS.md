# Arena 专用服务端说明

## 项目定位

本项目是 `windbot-arena` 的专用 SRVPro，使用 `server` 分支的 YGOPro。

项目不以兼容原版 SRVPro 为目标。修改时优先保证长期批量对局的稳定性和资源效率，只保留基础密码房及 Arena 实际使用的能力。

相关源码通常位于 `../windbot-arena` 和 `../mycard-ygopro`。

## 专用边界

- 配置、数据、资源和目录结构只服务于本项目；未使用的原版功能可以直接删除。
- 不要为原版配置或旧版专用配置增加兼容读取、自动迁移或废弃字段。配置结构改变后由部署者手动调整。
- 启动时合并 `data/default_config.json` 与用户配置，并将完整配置写入 `config/config.json`。
- 相同的非空密码必须进入同一房间，不同密码不得自动匹配。保留 `S#`、`M#`、`T#` 及现有自定义规则前缀；Arena 主要使用唯一的 `M#...` 房名。
- 保持 Arena 使用的房间查询、计数、排行、中途观战开关、关房和重启接口，以及稳定的 `serverInstanceId`。接口或认证方式变更时必须同步修改 Arena。

## 开发约束

- 使用现代原生 JavaScript 和 Node.js API，不再使用曾经的 CoffeeScript。
- 房间、客户端、子进程和观战连接必须通过统一且幂等的关闭流程释放，避免晚到事件访问已关闭或同名重建的房间。
- TCP 是字节流，协议转发必须正确处理拆包与粘包。
- `observer` 表示服务端的隐藏全信息连接，`watcher` 表示中途加入的观战客户端，`replay` 仅表示决斗录像。

## 验证重点

- 房名解析、密码房配对和胜率统计行为。
- 房间创建、关闭、同名重建及资源清理。
- 协议拆包、粘包和中途观战数据流。
- 管理认证、HTTP 接口及 `serverInstanceId`。
